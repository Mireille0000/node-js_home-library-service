services:
  home-lib-app:
      build:
        context: .
        dockerfile: Dockerfile-app
      container_name: nest-app
      ports:
        - 4000:4000
      depends_on:
        nest-postgres-serv:
          condition: service_healthy
      networks:
        - app-network
      environment:
        DATABASE_URL: postgresql://prisma:topsecret@nest-postgres-serv:5432/homelibdb?schema=public
        DEBUG: prisma:engine

  nest-postgres-serv:
    build:
      context: .
      dockerfile: Dockerfile-psql-db
    container_name: nest-postgres-serv
    environment:
      POSTGRES_USER: prisma
      POSTGRES_PASSWORD: topsecret
      POSTGRES_DB: homelibdb
    ports:
      - 5432:5432
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U prisma -d homelibdb"]
      interval: 10s
      timeout: 5s
      retries: 5
    # volumes:
    #   - nest-postgres-serv:/var/lib/postgresql/data

networks:
  app-network:
    driver: bridge


# services:
#   nest-postgres-serv:
#     image: postgres:14
#     container_name: nest-postgres-serv
#     restart: always
#     ports:
#       - '${DB_PORT}:${DB_PORT}'
#     env_file:
#       - .env
#     volumes:
#       - nest-postgres-serv:/var/lib/postgresql/data

#   home-lib-api:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     container_name: home-lib-api
#     restart: always
#     ports:
#       - '${PORT}:${PORT}'
#     environment:
#       DATABASE_URL: ${DATABASE_URL}
#     depends_on:
#       - nest-postgres-serv
#     env_file:
#       - .env

# volumes:
#   nest-postgres-serv:
#     name: nest-postgres-db
